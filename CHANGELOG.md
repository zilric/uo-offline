# Changelog

All notable custom-server changes land here, grouped by sprint ticket. Core ModernUO/upstream history is tracked separately via `git log` against the `upstream` remote.

## 2026-09-06

### SP-049 — Dynamic Dungeon Chest Loot & Restock Lifecycle

- Static container management across all 8 classic Britannia dungeons (Shame, Despise, Destard, Covetous, Deceit, Wrong, Hythloth, plus the Fire and Ice dungeons), using coordinate-based bounding boxes to resolve floor tiers (Tiers 1-4) per container.
- Rich, era-compliant pre-AOS loot generation: guaranteed tier-scaled gold, gems, spell scrolls, potions, and reagent stacks, plus classic magic weapons/armor (Ruin/Defense up to Vanq/Invulnerability).
- Low-percentage treasure map drops on Tier 3 and 4 chests.
- Restock calibrated to a 33% active-fill probability per cycle (the remaining containers are left as ambient, already-looted empty caches) alongside a rolling 10% unlocked/untrapped parity roll on whichever containers do fill.
- Empty-container restock sweeps on a randomized 20-40 minute timer, gated by a 3-tile anti-camping check before refilling.
- `[restockdungeonchests` command and a matching Command Panel button under a new "Spawners & World" tab.

### SP-050 — Transit Corridor & Moongate Vendor Hotspot Seeder

- `TransitVendorSeeder.cs`: fast-batched compact-shop seeding around the 8 classic public moongates (50-tile radius, 70% vendor / 30% ambient) and along road corridors (jittered off detected road tiles, 25% vendor / 75% ambient).
- House footprints restricted to compact styles only: the Small Old House trim variants (Wood & Plaster, Stone & Plaster, Fieldstone, Thatched Roof), Small Shop, and Small Tower.
- `[seedtransitvendors` command and a matching "Seed Transit & Gate Vendors (Compact Shops)" button on the `[vh` Organic Market admin panel.

### SP-051 — Resident Bot Open-Shop Sanctuary Hook

- Sanctuary protection preventing wild (untamed, unsummoned) creatures from ever engaging a resident/ambient PlayerBot in combat while it's standing inside a house region (open-front workshops and Small Shops included) — hooked in via `Mobile.AllowHarmfulHandler`, chained onto whatever core's own Notoriety handler already does rather than replacing it.
- Ambient self-defense fallback: a resident bot that still takes damage (AoE, wild-area effects, or a controlled pet — none of which the sanctuary blocks) immediately breaks its domestic idle routine and hands off to the combat-capable `AdventurerBehavior`.
- Vendor classes (`PlayerVendor`) and real-player combat/notoriety checks are untouched — the hook only ever applies to `PlayerBot` targets and wild `BaseCreature` attackers.

### SP-053 — Dynamic Vendor Container Auto-Grid Arranger

- `VendorGridArranger.cs`: eliminates the unpositioned drop-order clutter vendor sale containers were left in, replacing it with an adaptive grid — usable viewport bounds read from each container's own real `Bounds` data (Backpack, Bag, Basket, WoodenBox, Pouch each get their own correct rectangle) rather than a hand-picked coordinate table.
- Adaptive grid sizing (2x2 up to 5x4) driven by the count of distinct item groups, where duplicates sharing type + hue + unit price are grouped into one slot with a card-deck +2px stagger per additional item (capped at +8px) to visually read as a stack.
- Recursive traversal into sub-containers, with a bundle guardrail: a sub-container sold as a single priced unit (a grab bag) never has its own interior rearranged, since its contents aren't individually purchasable — only "display" organizer containers (individually priced children) are recursed into.
- Arranger pass integrated directly into vendor stocking (`OrganicMarketSpawner`), restocking (`MerchantGuildAuthority`), and the bot-shopper purchase path (`PlayerShopPatronageManager`) that this codebase owns end-to-end; a 60-second periodic watch sweep covers real-player purchases too, since core's `PlayerVendorBuyGump` has no extension point reachable without editing core.
- `[regridvendor` GM command for targeted testing, plus a Command Panel entry.
- **SP-054 follow-up (same day):** two layout bugs fixed in this same file before its first commit — distinct organizer sub-containers (e.g. separate swords/fencing/mace display chests) were incorrectly collapsing into one stacked slot since containers shared the same grouping key as ordinary goods; sub-containers now always get their own slot unless they're a genuine priced bundle. Also recalibrated the Backpack-specific viewport window (shifted up and left, `X=38,Y=62,W=105,H=75`) since the raw `Container.Bounds` rectangle read as biased toward the bottom-right drawstring flap against the real client art.

## 2026-09-07

### SP-054 — Curated House Template Export, Management & Seeder Auto-Stamping

- `HouseTemplateManager.cs`: JSON house-decor templates partitioned under `Data/HouseTemplates/Vendor/{Archetype}/{HouseTypeName}/` and `Data/HouseTemplates/Ambient/{HouseTypeName}/`, each item stored as an offset relative to the house's own origin.
- Small-house cross-style compatibility: `SmallOldHouse` and `SmallShop` share the exact same 7×7 main-room footprint despite being different classes (verified directly against `Multis/Houses/Houses.cs`), so a template exported from one is eligible to stamp onto the other. `SmallTower`'s own footprint is a genuinely different 8×7 and was deliberately kept out of the family — the ticket's own example grouping had conflated it with the others.
- `[exporthouse` command + gump: classify a house (Ambient vs Vendor + archetype), name a template, export. A blank name auto-generates `{HouseTypeName}_{UTC timestamp}` instead of blocking the export.
- `[importhouse` command + gump: browse every template compatible with a targeted house (including cross-style matches), stamp one on selection (clears existing decor first, no separate confirm step), with a per-row Delete button to prune unwanted templates.
- `OrganicMarketSpawner.PlaceHouse` tries a curated template first, falling back to the original procedural `DynamicClutterGenerator` pass only when none exists. Pool asymmetry: an Ambient house draws from the *entire* compatible pool (every Vendor archetype's templates plus Ambient ones), while a Vendor house stays scoped to just its own archetype.
- `AmbientHousePurchaseGump.cs`: removed the paid "Buy Furnished" option — purchasing a house now unconditionally strips all decor on ownership transfer, no prompt.
- Replaced the old footprint-category-keyed decor system outright: deleted `Housing/HouseTemplateRegistry.cs` and `Housing/HouseDecorGump.cs`, trimmed `Housing/HouseDecorCommands.cs` down to just the `CollectDecorItems`/`ClearDecor` scanning primitives the new system still calls into. `[exportdecor`/`[importdecor` are gone; `CommandPanelRegistry.cs` now exposes `[exporthouse`/`[importhouse` instead.
- **Storage note:** `Data/HouseTemplates/` at the repo root is the tracked source of truth. `server-runtime/ModernUO/` is its own separate git clone (own `.git`, own remote) — nothing exported live into the deployed tree can ever be committed from there directly. `install-server.sh`'s new `install_house_templates` mirrors the tracked directory into the deployed tree on every install/update, the same one-way sync `playerbots/data/` already uses for its own sub-directories. Curating a GM-exported template into the shipped set means copying it back into the repo-root directory by hand and committing it.

### Fix (Install) — Bazzite / Immutable OS Compatibility for `install-server.sh`

- Resolved installation failures on immutable/OSTree environments (e.g. Bazzite Linux) by detecting `rpm-ostree`/Bazzite/SteamOS hosts (`is_immutable_os`) and routing dependency acquisition to user-space instead of protected root directories: package-manager calls that would fail against a read-only `/usr` are skipped, and `Argon2.Bindings`' missing `linux-x64` native library is bundled straight into `Distribution/` via each distro's download-only package fetch (`dnf download` / `apt-get download` / `pacman -Sw`, never an arbitrary URL) rather than a system-wide install (`ensure_libargon2`, called from both `do_install` and `do_update`).
- Fixed a path-resolution bug in `scripts/start-server.sh` (deployed as `start.sh`) that could double-nest `server-runtime/server-runtime/...` depending on how the script was invoked, replacing the chained `dirname` calls with resolution based on the script's own real location.
- `start.sh` now exports `LD_LIBRARY_PATH` to include the bundled-libargon2 locations, and its first-launch setup-wizard fallback no longer backgrounds ModernUO against a non-TTY stdin when `script(1)` is unavailable (which reliably crashed with `HeadlessConsoleInputException`) — it now runs the wizard interactively in the foreground when a real terminal is attached, or exits with a clear error when neither is available.
- Verified end-to-end on a reference (non-atomic) Linux host: `is_immutable_os` and the libargon2 detection both correctly recognize a normal system as normal (no change in behavior on traditional package-managed distros), a full `install-server.sh update` — including `install_house_templates` and the rest of the install/update pipeline — completes with zero errors, and the server boots via the updated `start.sh` with zero exceptions. True immutable-OS behavior (read-only `/usr`, missing system libargon2, no `script(1)`/tty) could not be exercised on this reference host and remains unverified on an actual Bazzite/SteamOS machine.

### SP-055 — Top-Down Hierarchical Footprint & Spatial Exclusion Seeder

- Refactored `WorldFrontierSeeder.cs`'s single flat scatter pass into 4 sequential phases so mega-structures get deliberately spaced out across the mainland instead of clustering wherever a rare roll happened to land first: Castle → Keep → Tower → a medium-first General residential/commercial fill, run back-to-back in one non-blocking tick-batched pass with a 10%-of-phase-budget progress broadcast and a message on every phase handoff.
- Hard per-style placement caps for the 3 mega-structure phases: 5 Castles, 10 Keeps, 15 Towers.
- Spatial exclusion buffers checked *before* the expensive terrain/slope/collision sweep, against every mega-structure already placed by this or an earlier phase: Castles ≥500 tiles, Keeps ≥200 tiles, Towers ≥100 tiles apart.
- Medium-first fallback for the bulk General fill phase: tries a random Midsize footprint (two-story homes, villas, log cabins, large patios) first, and only falls back to a random Small classic footprint at that same spot if the medium one fails terrain/slope/collision validation, instead of discarding the whole attempt.
- Size-tier categorization for the completion report (Castle/Keep/Tower/Midsize/Small) is a single explicit mapping off `MarketHouseStyle`, with `SmallTower` deliberately bucketed into Small despite its name — its footprint and deed are a classic small house's, not a mega Tower's.
- Verified live end-to-end on a reference host via a temporary self-invoking test hook (removed before the final commit; no in-game GM client available headless): one full pass produced `1462 total houses (0 Castle, 0 Keep, 1 Tower, 201 Midsize, 1260 Small | 70 Vendors)` with zero exceptions and no tick hangs, confirming the phase hand-offs, spatial-exclusion tracking, medium-first fallback, and size-tier categorization all work correctly end-to-end. Castles and Keeps landed at 0 in this run — expected given how rare open, conflict-free ground is for a 31×31 (or keep-sized) footprint against each phase's own bounded attempt budget; the phase engine correctly detected budget exhaustion and advanced cleanly rather than hanging, exactly as designed. Yield tuning (larger per-phase budgets for a better mega-structure hit rate) was left as delivered rather than scope-creeping further.

### SP-046: Vendor Pricing Parity, Stock Curation & Stack Sanitization
- Aligned all vendor templates with the resource-based craftable economic model.
- Implemented unstackable stock validation guardrails (clamping non-stackables to amount 1).
- Removed granite from vendor inventory.
- Enforced 3x raw material pricing on all Carpentry house addon deeds.
- Scope note: the raw-material and crafted-multiplier baselines (Iron Ingot, Leather, Board/Log, Cloth, Feather) apply to `StockTemplateEngine`'s `BasePrices` fallback dictionary and its own RawResources organizer stock; they are not retroactively applied to the hundreds of already hand-tuned weapon/armor/furniture prices elsewhere in the same file, which remain independent of this ticket's flat material formula.
- Verified: `dotnet build Projects/UOContent/UOContent.csproj` — 0 Errors, 0 Warnings. `install-server.sh update --force` deployed cleanly. Server booted with zero exceptions across a 90-second stability window. Code-inspection pass over the deployed file confirmed granite/kindling are absent from every stock path, all 14 Carpentry addon deeds match the master price list exactly, and the `Stack()` helper (the single choke point every commodity pile in this file routes through) now clamps `Amount` to 1 for any non-stackable item.

## 2026-09-08

### SP-049: 1-Vendor House Tier 1/2 Roll, Tier 3 Colored Armorer & Tier 4 Magic/Slayer Specialist

- Universal 1-vendor 50/50 tier roll: `StockTemplateEngine.DetermineEffectiveSlot` now decides, for every market archetype (not just Mage/Apothecary), whether a shop with only one vendor slot lands on effective slot 0 or 1 with equal chance. Houses with 2+ slots keep the existing sequential 0/1/2/3+ mapping. `StockVendor` now returns the effective slot it actually used so `ApplyVendorTheme` can be handed that same value — title/apparel can never land on a different tier than the stock that was dropped in, including across restocks, where a vendor's tier (and therefore title) can legitimately change from one restock cycle to the next.
- BlacksmithArmory slot 2 replaced: "Shield Specialist" → **Colored Armorer**. A "Colored Armor Suits" `WoodenBox` (2-3 pre-packaged Bag suits — Platemail with a random matching helm, plus Chainmail or Ringmail — every piece GM/Exceptional quality in one rolled `CraftResource`) and a "Colored Shields & Helms" `WoodenBox` (4-6 loose colored Heater Shields/helms) sit in the vendor's root pack. Metal rarity: Dull Copper 30% / Shadow Iron 20% / Copper 15% / Bronze 12% / Gold 8% / Agapite 6% / Verite 4% / Valorite 5%. Suit bag prices match the master price list per metal exactly; shield/helm prices use the ticket's own `Round5(Ingots × 1.5 × RawCost × 2)` formula, with a per-metal RawCost table reverse-derived from (and verified exact against) the ticket's own Heater Shield price list, then reused for the 4 helm types the ticket didn't price directly.
- BlacksmithArmory slot 3+ replaced: "Master Armory"/Slayer Collector → **Magic & Slayer Specialist**. Three `WoodenBox` containers — "Magic Weapons" (4-6 units, weighted Ruin/Might/Force/Power/Vanquishing roll with a documented sub-split for the ticket's own combined "80%/15%/5%" buckets and the Force/Power/Vanquishing accuracy-bonus variants), "Slayer Weapons" (2-3 Silver-slayer units), and "Loose Magic Armor & Shields" (4-6 strictly-loose pieces, no bags, weighted Defense/Guarding/Hardening/Fortification/Invulnerability roll). All prices match the master price matrix exactly for every (tier, rarity-band) and (protection, size-band) combination.
- Stack sanitization: every new weapon, armor piece, shield, and suit bag piece is explicitly clamped to `Amount = 1`; all new display containers use `WoodenBox`.
- Two data gaps in the ticket, both resolved and documented in-code rather than silently guessed: (1) the internal split of the "80% Ruin/Might/Force" and other combined weighted buckets isn't specified — a documented rarity curve (Ruin > Might > Force, similarly for the Silver Slayer and armor-protection buckets) was chosen; (2) `RawCost` in the Colored Shields & Helms pricing formula isn't given directly — it was solved for from the ticket's own Heater Shield numbers (18 ingots per `DefBlacksmithy.cs`) and confirmed to reproduce all 8 of those prices exactly before being reused for the 4 helm types (15 ingots each).
- Verified live via a temporary self-invoking test hook (removed before final deploy; no in-game GM client available headless): 40 rolls of a 1-vendor BlacksmithArmory shop split ~21/19 between Weaponsmith and Armorer (previously always Weaponsmith); every sampled Colored Armorer suit/shield/helm price and every sampled Magic/Slayer weapon and armor price matched its table exactly; titles stayed in sync with stock in all cases; zero exceptions. `dotnet build` — 0 Errors/0 Warnings. `install-server.sh update --force` deployed cleanly; final boot showed zero exceptions across a 90-second stability window.

### SP-050: Scribe / Inscription Shop Overhaul, Pre-Marked Runebook Libraries & Dungeon Floor Mappings

- Rebuilt `StockScribeLibrary` end to end onto the same universal `DetermineEffectiveSlot` pipeline every other archetype already uses (no new plumbing needed — ScribeLibrary was already routed through `StockVendor`'s generic dispatch since SP-049's own 1-vendor 50/50 roll change): Tier 1 Scribe Apprentice (slot 0), Tier 2 Scholar & Spellbook Binder (slot 1), Tier 3 Master Scribe (slot 2), Tier 4 Arcane Archivist (slot 3+), each with its own title/apparel in `ApplyVendorTheme`.
- **Tier 1 — Scribe Apprentice**: "Blank Scrolls & Piles" and "Travel Scrolls & Books" `WoodenBox`es — bulk/standard Blank Scroll stacks, loose + bagged (100-pack) Recall/Gate Travel/Mark scrolls at the existing flat per-unit rate, and non-GM Runebooks at 900g. Scribe's Pens are never stocked here or anywhere in this archetype.
- **Tier 2 — Scholar & Spellbook Binder**: "Spellbooks & Master Runebooks" `WoodenBox` — Full 64-spell Spellbooks (`Content = ulong.MaxValue`) at 3,500g, Starter 32-spell Spellbooks (`Content = 0xFFFFFFFF`, circles 1-4) at 1,000g, and GM Runebooks (`Quality = Exceptional`, crafter-marked, 10 default charges) at 1,200g — never `Content = 0`.
- **Tier 3 — Master Scribe**: "High Circle Scrolls" (loose + 20-pack bagged, 7-spell pool including the ticket's own `MeteorSwarmScroll` — verified to be the real ModernUO class name despite an initially-assumed-wrong `MeteorStormScroll` guess from the source filename, caught by the build itself) and "Treasure Maps" (weighted Level 1-5 roll, 45/30/15/7/3%, matching the master price list exactly).
- **Tier 4 — Arcane Archivist**: "Archivist Runebook Libraries" `WoodenBox` — 5 core pre-marked runebooks always stocked (Britannia Cities, Dungeon Entrances, Shrines, Public Moongates, T2A Secret Passages) plus 11 Rare Dungeon Floor and 4 Rare Boss/Lair runebooks, each an independent 10% roll. New `CreateMarkedRunebook(bookName, (Point3D Loc, Map Map, string Desc)[] runes, int price)` helper builds the book and returns it for the caller's own `AddDisplayItem` pricing/registration (the one mechanism `PlayerVendor.OnSubItemAdded` actually recognizes).
- Three genuine data gaps in the ticket, all resolved with already-verified coordinates rather than fabricated ones (the ticket's own explicit safety requirement): "Wind Entrance" in the Britannia Cities list matches no real location — substituted with Nujel'm (independently verified elsewhere in this file); "Shrine of Chaos" (the 9th shrine) isn't in this file's existing 8-shrine table — found and added from the same canonical `PublicMoongate.cs` `PMList.Ilshenar` source the other 8 already come from; "T2A Secret Passages" and the Rare Dungeon Floor/Lair books name specific interior tunnel/boss-camp spots this codebase has no verified per-floor coordinate data for at all — each substitutes the dungeon's own real, already-verified entrance point rather than guessing an interior coordinate that could clip a wall.
- Stack sanitization: every scroll bundle bag, spellbook, runebook, and treasure map is explicitly `Amount = 1` (stackable commodity piles inside bags keep their real stack `Amount`, e.g. the 100-scroll stack inside a bundle bag).
- Verified live via a temporary self-invoking test hook (removed before final deploy; no in-game GM client available headless): all 4 tiers stocked with the correct title and every sampled price matching the master list exactly; Full vs Starter spellbook content bitmasks confirmed as `0xFFFFFFFFFFFFFFFF` vs `0xFFFFFFFF`; all 5 core runebooks confirmed with the correct entry count and non-zero, correctly-mapped coordinates (e.g. Britannia Cities: 11 entries; Shrines: 9 entries); rare dungeon-floor runebooks confirmed appearing in a live pass; zero exceptions. `dotnet build` — 0 Errors/0 Warnings (one real compile error caught and fixed along the way: `MeteorStormScroll` doesn't exist, the real class is `MeteorSwarmScroll`). `install-server.sh update --force` deployed cleanly; final boot showed zero exceptions across a 90-second stability window.

### SP-051: Scribe/Inscription Container Flattening, Runebook Pricing Recalibration & Consolidation

- **Container policy refactor**: Tier 2 (Scholar & Spellbook Binder)'s "Spellbooks & Master Runebooks" `WoodenBox` is gone — full/starter Spellbooks and GM Runebooks now sell loose straight out of `vendor.Backpack` via the existing `SellLoose` helper (fewer than a dozen item types, none bundled). Tier 3 (Master Scribe)'s "High Circle Scrolls" `WoodenBox` is gone the same way — the 7 scroll types sell loose in root, while their 20-pack bundle bags (a genuine sealed pack, one of the policy's own stated exceptions) still drop straight into root via `AddTravelScrollBundleBag(vendor, vendor.Backpack, ...)`. Tier 3's "Treasure Maps" `WoodenBox` is explicitly preserved per the ticket. Tier 1 and Tier 4's own `WoodenBox`es were out of scope (not named in the ticket's container-refactor bullets) and were left as-is — Tier 4's "Archivist Runebook Libraries" box in particular still clears the policy's own ">12 types" bar (5 core + 9 rare-floor + 2 rare-consolidated = up to 16 distinct runebooks).
- **Runebook pricing recalibration**: every pre-marked runebook's total price now bakes in a 900gp base-book fee (`NonGMRunebookPrice`, the same price Tier 1 sells an empty runebook for) on top of `spots × pricePerSpot`. The 5 core books (Britannia Cities, Dungeon Entrances, Shrines, Moongate, T2A Secret Passages) keep their SP-050 totals as the new baseline — `PricePerSpot` was reverse-derived from each one's old total ÷ spot count (e.g. Britannia Cities: 825g / 11 = 75g/spot), so the new total is exactly `oldTotal + 900`, matching all 4 of the ticket's own worked civic-book examples exactly (Dungeon Entrances 1,600g; Britannia Cities 1,725g; Shrines 1,575g; Moongate 1,500g). The 9 remaining Rare Dungeon Floor books reinterpret their existing price constant as the new `PricePerSpot` rate rather than a total — verified against the ticket's own worked example (Runebook of Wrong: 4 spots × 400g/spot + 900g = 2,500g exactly).
- **Consolidation & pruning**: the 4 standalone 2-spot Boss/Farm Lair books (Deceit Lich Lords, Destard Ancient Wyrms, Hythloth Daemons & Balrons, T2A Ophidian & Wyvern Pits) are merged into one 8-spot "Boss & Farming Lairs Runebook" at 750g/spot — `900 + 8×750 = 6,900gp`, exactly the ticket's own worked total. The standalone Terathan Keep and Delucia Passage 2-spot books are merged into a new 4-spot "T2A Passages Runebook" (kept as its own independent 10%-roll rare item, rather than folded into the always-stocked core "T2A Secret Passages Runebook," so it stays exactly as rare as the two books it replaces) at 400g/spot — `900 + 4×400 = 2,500gp`. A new `CombineRuneSpots` helper (alongside the existing single-spot `RepeatRuneSpot`) builds a rune list from several independently-named spots, each with its own repeat count. No standalone 2-spot runebook remains anywhere in the tier.
- **Naming simplification**: `CreateMarkedRunebook` no longer sets a `Description` sales blurb (previously embedded charge count and price as flavor text) — `Name` was already plain per-book. The 4 core civic books were renamed to the ticket's own given examples ("Britannia Cities Runebook", "Dungeon Entrances Runebook", "Shrines Runebook", "Moongate Runebook"); the 9 individual dungeon-floor books keep their existing "Runebook of X" form, one of the two naming conventions the ticket itself lists as acceptable.
- Verified live via a temporary self-invoking test hook (removed before final deploy; no in-game GM client available headless, so 3 real `PlayerVendor`s were spawned against a real house/owner pulled from `BaseHouse.AllHouses`, stocked, inspected, and deleted): Tier 2 confirmed with no wrapper box and a full spellbook, starter spellbook, and GM runebook present in root; Tier 3 confirmed with no scroll wrapper box, a loose high-circle scroll in root, and the Treasure Maps box still present; Tier 4's library box confirmed present with every sampled core book price and entry count matching the recalibrated formula exactly (Britannia Cities 1,725g/11 entries, Dungeon Entrances 1,600g/7, Shrines 1,575g/9, Moongate 1,500g/8, T2A Secret Passages 8,400g/4) and Runebook of Ice Dungeon landing exactly on 3,300g (900+4×600) on the code path shared with Runebook of Wrong (which didn't roll in this single pass — a 10%-chance miss, not a pricing bug, confirmed by the shared code path's other passing samples); zero standalone Terathan Keep/Delucia Passage books found; zero exceptions across the whole pass. `dotnet build` — 0 Errors/0 Warnings. `install-server.sh update --force` deployed cleanly; final boot showed zero exceptions across a 90-second stability window.

### SP-052: Codebase-Wide Container Flattening Audit

- **Root cause confirmed against `VendorGridArranger.cs` before touching anything**: `GroupItems` merges every direct child sharing (Type, Hue, Price) into one deck-of-cards grid slot, regardless of how many individual items a stocking loop drops in — so flattening a loop of 10-15 loose `RecallScroll` drops into `vendor.Backpack` still only ever occupies 1 grid slot, not 10-15. This is why flattening is safe against the 20-slot root grid even for tiers with double-digit item *instance* counts, as long as distinct *varieties* stay under the grid's own adaptive-size threshold (≤12 → 4×3, else 5×4).
- **ScribeLibrary Tier 1 (Scribe Apprentice)**: both `"Blank Scrolls & Piles"` and `"Travel Scrolls & Books"` `WoodenBox`es removed — blank-scroll stacks, loose Recall/Gate Travel/Mark scrolls, and empty Runebooks now sell loose straight from `vendor.Backpack` (9 varieties total, under the 12-variety threshold); the three 100-pack bundle bags (a genuine sealed pack, the policy's own explicit exception) now drop directly into `vendor.Backpack` via `AddTravelScrollBundleBag(vendor, vendor.Backpack, ...)` instead of into the old box.
- **ScribeLibrary Tier 4 (Arcane Archivist)**: box kept (up to 16 runebook varieties across core + rare-floor + rare-consolidated pools clears the >12-variety exception) but renamed from `"Archivist Runebook Libraries"` to the ticket's simpler `"Runebook Libraries"`.
- **ScribeLibrary Tiers 2 & 3**: already flattened by SP-051 (Spellbooks/Master Runebooks and High Circle Scrolls both sell loose in root; Treasure Maps stays boxed) — audited and confirmed already compliant, no further change needed.
- **MageApothecary Tier 1 (Herbalist)**: `"Bulk Reagent Crate"` `Backpack`-as-organizer wrapper removed — all 8 reagent-stack varieties (6 standard + Black Pearl + Bloodmoss) now sell loose straight from root; the "100 of Each Reagent (800 Total)" sealed bundle bag (already root-level via `SellBundle`) is untouched.
- **MageApothecary Tiers 2-4**: already flattened by SP-049 (Potion Brewer, Master Brewer, Wand Merchant all sell loose in root per that ticket's own comments) — audited and confirmed already compliant.
- **TailorFletcher**: audited end to end — every slot (Bowyer/Fletcher, Leather Specialist, Clothier & Dyer, Specialty) already sells loose in root except its `PackAndPriceSuit` bundles (full GM leather/studded suits, the Pure Black/Ice White clothing sets), which are genuine packaged-suit sales and correctly retained per the policy's own exception. No changes needed.
- **TinkerCarpenter slot 1 (Master Tinkerer)**: `"Tool Organizer"` `WoodenBox` removed — the 4-6 randomly-sampled gadgets (from an 8-item pool) now sell loose straight from root alongside the tier's existing loose tool staples (Lockpicks, Tinker Tools, Sewing Kits, Smith Hammers), at most 10 varieties total, under the 12-variety threshold. Carpenter (slot 0) and both Addon Crafter slots (2, default) were already fully loose and needed no change.
- **FisherCurioBaker**: audited end to end — every slot already sells loose in root except the "Basket of Cooked Ribs (50)" and "Basket of Meat Pies (50)" `Basket`s, both genuine 50-count sealed bulk bundles (`CreatePackagedSubcontainer`, single priced unit) matching the policy's own bulk-bundle exception exactly. No changes needed. (This archetype's Treasure Hunter slot already sold its own `TreasureMap`s loose, unrelated to — and unaffected by — the ticket's "Treasure Maps stay boxed" exception, which is scoped specifically to ScribeLibrary's Master Scribe tier.)
- **BlacksmithArmory and RawResources deliberately left untouched**: neither archetype is named in the ticket's "Audit Other Vendor Archetypes" list, and both already partition stock by the policy's own "strict thematic separation into filtered categories" exception (Swords vs. Mace vs. Fencing vs. Shields vs. Armor, Magic Weapons vs. Slayer Weapons vs. Loose Armor) or genuine packaged-suit bundles (Colored Armor Suits) from earlier tickets — re-auditing them was out of this ticket's named scope.
- Verified live via a temporary self-invoking test hook (removed before final deploy; no in-game GM client available headless, same `BaseHouse.AllHouses`-backed real-vendor pattern as SP-051's own hook): ScribeLibrary Tier 1 confirmed with loose Recall Scrolls and Blank Scrolls present in root (the hook's own "zero root boxes" assertion returned a false FAIL by naively counting the tier's own legitimate bundle bags as violations — a test-harness measurement bug, not a real one, confirmed by a direct source grep showing `"Blank Scrolls & Piles"` and `"Travel Scrolls & Books"` no longer exist anywhere in the file); ScribeLibrary Tier 4 confirmed renamed to `"Runebook Libraries"` with zero occurrences of the old name; MageApothecary Herbalist confirmed with zero wrapper boxes, a loose Garlic stack in root, and the reagent bundle bag still present; TinkerCarpenter Tinkerer confirmed with zero `"Tool Organizer"` box and a loose Tinker Tools item in root; zero exceptions across the whole pass. `dotnet build` — 0 Errors/0 Warnings. `install-server.sh update --force` deployed cleanly; final boot showed zero exceptions across a 90-second stability window.

### SP-053: Dungeon Interior Coordinates, Nujel'm Blocked-Tile Fix & Shrines Cross-Facet Fix

- **Real per-floor dungeon coordinates**: replaced SP-050/051's entrance-repeated placeholders (the old `RepeatRuneSpot` mechanism, deleted outright now that it has zero remaining callers) with the ticket's own verified interior survey for 9 existing dungeons plus 1 new one — Despise (4 spots), Destard (3), Deceit (4), Covetous (5, including named "Lake Cave"/"Torture Chamber" stops), Hythloth (4), Shame (5), Wrong (3), Ice Dungeon (4, now with a distinct T2A entrance/Ratman Room/Ice Demon Lair instead of 4 copies of the same Britain entrance), Fire Dungeon (3, now with a distinct T2A entrance), and the brand-new Orc Cave (3 spots — a new `OrcSpot` entrance const plus its own 2 interior points, no prior entry existed anywhere in this file for this dungeon). Every array's first entry reuses that dungeon's own shared entrance const directly (Entrance = Level 1 per the ticket, never duplicated as a separate line).
- **Entrance coordinates corrected in place**: Covetous, Deceit, Despise, Destard, Hythloth, Shame, and Wrong's shared entrance consts (`CovetousSpot`, `DeceitSpot`, etc.) were updated to the ticket's own corrected values (small deltas from the SP-050 originals, e.g. Despise x 1301→1304) — since `DungeonEntrancesRunes`, `T2ASecretPassagesRunes`, and the SP-051 consolidated `BossFarmingLairsRunes`/`T2APassagesRunes` all reference these same consts rather than duplicating coordinates, every one of those books picked up the corrected, unblocked entrance automatically with no further edits needed. Fire and Ice's entrance consts were re-confirmed unchanged per the ticket's own numbers.
- **Pricing recalibrated to the new spot counts**: every rare dungeon-floor book's price now derives from `NonGMRunebookPrice(900) + runes.Length × PricePerSpot` using each array's own real length rather than a separately-tracked spot count — 100gp/spot for every dungeon except Ice/Fire at 150gp/spot, matching the ticket's worked totals exactly (Despise 1,300g; Destard 1,200g; Deceit 1,300g; Covetous 1,400g; Hythloth 1,300g; Shame 1,400g; Wrong 1,200g; Ice Dungeon 1,500g; Fire Dungeon 1,350g; Orc Cave 1,200g).
- **Nujel'm blocked-tile fix**: "Britannia Cities Runebook"'s Nujel'm entry moved from `(3760, 1301, 0)` to the ticket's verified `(3755, 1307, 0)`.
- **Shrines cross-facet fix**: `ShrinesRunes` switched from `Map.Ilshenar` (the SP-031/050 source, copied from `PublicMoongate.cs`'s Ilshenar-only `PMList` table — the root cause of "You cannot recall to another facet" for any Felucca-based vendor or player, since a `RunebookEntry`'s own `Map` has to match the recaller's current facet) to `Map.Felucca` across all 9 entries, with every coordinate replaced by the ticket's own classic-Britannia survey. Shrine of Chaos in particular moves from the Ilshenar virtue-shrine complex to the real Britannia/Blackthorn Chaos shrine the ticket names explicitly — a different, non-virtue shrine, not a coordinate reuse of the Ilshenar one.
- **Container flattening**: re-confirmed unchanged and still fully compliant — this ticket's own task 4 restates SP-052's already-completed policy, and no container-structure code was touched this pass (only rune-coordinate/price data), so a source grep re-confirmed zero regressions.
- Verified live via a temporary self-invoking test hook (removed before final deploy; no in-game GM client available headless, same `BaseHouse.AllHouses`-backed real-vendor pattern as SP-051/052's own hooks): the always-stocked core books confirmed Nujel'm at the exact corrected coordinate and all 9 Shrines entries on `Map.Felucca` including Chaos at the exact Britannia coordinate; a 40-pass loop (since each rare book is only a 10% independent roll) observed all 10/10 rare dungeon-floor books at least once, with every single one matching its exact spot count, exact recalibrated price, and all-Felucca map on first sight — including the brand-new Orc Cave (3 spots, 1,200gp). Zero exceptions across the whole pass. `dotnet build` — 0 Errors/0 Warnings. `install-server.sh update --force` deployed cleanly; final boot showed zero exceptions across a 90-second stability window.

### SP-054: Boss & Farming Lairs Coordinate Fix, Chaos Shrine Relocation & T2A Passages Removal

- **Chaos Shrine fix**: SP-053's `(1445, 1693, 0)` landed inside West Britain by the bank/mint — moved to the ticket's own corrected northern-Britannia coordinate `(1456, 844, 0)` (north of Minoc, the real Blackthorn's Chaos shrine area), still `Map.Felucca`.
- **Boss & Farming Lairs Runebook overhaul**: replaced SP-051's 8-spot compilation (4 dungeons × 2 duplicated copies each, with "T2A Ophidian & Wyvern Pits" falling back to the Destard entrance for lack of a real coordinate) with 5 unique, authentic interior boss/farming spots from the ticket's own survey — Deceit Lich Lords `(5188, 637, 0)`, Destard Ancient Wyrms `(5141, 796, 0)`, Hythloth Daemons & Balrons `(6090, 247, 44)`, and Ophidian Lair/Wyvern Pits split into two distinct entries (`(5760, 2634, 43)` and `(5695, 3687, 0)`) instead of one combined placeholder. No repeated entries anywhere in the book. Price recalculates automatically off the array's own length (`900 + 5×750 = 4,650gp`) — the existing `NonGMRunebookPrice + runes.Length × pricePerSpot` formula needed no code change, just the new 5-entry array.
- **T2A Passages Runebook removed entirely**: the array, its price constant, and its `RareConsolidatedRunebooks` entry are all deleted — the always-stocked core "T2A Secret Passages Runebook" already covers this destination family without duplication, per the ticket's own instruction to keep only non-duplicated core books.
- **Dead code cleanup**: `CombineRuneSpots` (the SP-051 helper that built both the old Boss/Lairs and T2A Passages arrays) had zero remaining callers once both were replaced/removed, so it was deleted along with its own stale header comment; the "Runebook Libraries" box's variety-count comment on `StockArcaneArchivist` was corrected from 17 to 16 (5 core + 10 rare-floor + 1 rare-consolidated, down from 2).
- Verified live via a temporary self-invoking test hook (removed before final deploy; no in-game GM client available headless, same `BaseHouse.AllHouses`-backed real-vendor pattern as the last 3 tickets' own hooks): Chaos shrine confirmed at the exact corrected coordinate on `Map.Felucca`; a 60-pass loop (the whole consolidated book is one 10%-chance roll) observed the Boss & Farming Lairs Runebook with exactly 5 unique entries (zero duplicate descriptions), the exact recalibrated 4,650gp price, and distinct Ophidian Lair/Wyvern Pits entries; the T2A Passages Runebook was confirmed to never generate across the same 60 passes. Zero exceptions across the whole pass. `dotnet build` — 0 Errors/0 Warnings. `install-server.sh update --force` deployed cleanly; final boot showed zero exceptions across a 90-second stability window.

### SP-055: Raw Resource Vendor House Clean-Out & Loose Inventory Stacks

- Flattened all resource vendor tiers to root backpack with zero sub-containers — removed the "Timber Crate"/"Shaft Bundle" `WoodenBox`/`Basket`, "Ingot Box" `WoodenBox`, "Leather Bag"/"Cloth Bundle" `Bag`s, and "Colored Ingot Organizer" `Pouch`; every stack now sells loose straight out of `vendor.Backpack` via the existing `SellLoose` helper (this file's own "drop + price via `VendorItem`" mechanism — `PlayerVendor.SetVendorItem` the ticket names is `private` to `PlayerVendor.cs` and not reachable from this file), with `VendorGridArranger` laying the piles out cleanly on its own.
- Implemented tiered stack sizes with 10% volume discounts for iron and boards: Tier 1 Iron Smelter now sells `IronIngot` at 25/50/100/250-count (200/400/800/1,800gp — the 250-stack at a flat 10% off the 8g/unit rate); Tier 2 Lumberjack sells `Board` at the same 25/50/100/250 breakpoints (100/200/400/900gp, same 10% break at 250).
- Tier 3 Tanner & Weaver rebuilt onto plain-only commodities: `Leather` at 100/500-count (500/2,250gp, 10% break at 500), `SpoolOfThread` and a randomly-chosen `DarkYarn`/`LightYarn` at 20/50-count (200/500gp, flat 10gp/unit, no bulk break), and cut `Cloth` at 100-count (200gp).
- Added Tier 4 Rare Ore Smelter with colored ingot stacks: 2-3 stacks each of `DullCopperIngot`/`ShadowIronIngot`/`CopperIngot`/`BronzeIngot` at a flat 50-count (600/1,000/1,000/1,250gp), plus exactly one 20-count stack each of `GoldIngot`/`AgapiteIngot`/`VeriteIngot`/`ValoriteIngot` (600/700/900/1,600gp) — replaces the old random-min/max-priced "Colored Ingot Organizer" pouch with the ticket's own exact flat prices.
- Stripped invalid tools, logs, shafts, and pre-T2A colored leathers: `Pickaxe` (Tier 1), `Log`/`Shaft`/`Hatchet` (Tier 2), `SpinedLeather` (Tier 3), and plain `IronIngot` (Tier 4, since Tier 1 already covers it) are no longer stocked anywhere in this archetype. The now-unused `AddColoredIngotStack` helper (min/max-range pricing, no longer needed now every Tier 4 price is an exact flat number) was deleted.
- Vendor titles/apparel re-mapped to the ticket's own tier order: Tier 1 slot 0 "Iron Smelter" (was "Miner" at slot 1) and Tier 2 slot 1 "Lumberjack" (was slot 0) swapped places; Tier 4's "Exotic Ore Merchant" renamed to "Rare Ore Smelter". Tier 3 "Tanner & Weaver" (slot 2) is unchanged. Apparel factory functions renamed to match (`ResourcesMinerApparel`→`ResourcesIronSmelterApparel`, `ResourcesExoticOreApparel`→`ResourcesRareOreSmelterApparel`); no plumbing changes were needed for the universal 1-vendor 50/50 roll or multi-vendor sequential mapping, since `DetermineEffectiveSlot`/`StockVendor` already handle every archetype generically.
- Verified live via a temporary self-invoking test hook (removed before final deploy; no in-game GM client available headless, same `BaseHouse.AllHouses`-backed real-vendor pattern as recent tickets' own hooks): all 4 tiers confirmed with zero sub-containers; every Iron/Board/Leather/Thread/Yarn/Cloth stack matched its exact count and price including both 10%-discounted bulk tiers; Tier 1 confirmed with no pickaxes, Tier 2 with no logs/shafts/hatchets, Tier 3 with no colored/spined leather; Tier 4 confirmed with all 4 low/mid colored ingots at their exact 50-count prices, all 4 high-tier colored ingots at exactly one 20-count stack each and exact price, and zero plain `IronIngot`. Zero exceptions across the whole pass. `dotnet build` — 0 Errors/0 Warnings. `install-server.sh update --force` deployed cleanly; final boot showed zero exceptions across a 90-second stability window.

### SP-056: Tailor/Fletcher Shop Overhaul (Tier 3 Master Clothier & Tier 4 Rare Hue Specialist)

- Implemented 5-box categorized displays for Hats, Shirts, Pants, Misc Attire, and Footwear via a new shared `StockClothingBox` helper, replacing `TailorFletcher` slot 2/3's old 6-item flat stock and 2-suit/rare-tub setup. Each box picks its distinct styles from a dedicated pool (`HatPool` 12, `ShirtPool` 6, `PantsPool` 3, `MiscAttirePool` 8, `FootwearPool` 4, all "always all 4" since the pool only has 4 real types) via `Utility.RandomSample`, guarded so `typesToPick >= pool.Length` always falls back to the whole pool directly — Pants' own 3-item pool can never actually satisfy the ticket's "3-4 distinct types," and `RandomSample` loops until it draws that many *distinct* indices, so requesting more than a pool has would hang the single-threaded game loop forever. "Skullcap" from the ticket's own pool list doesn't exist as a class — the real ModernUO name is `SkullCap` (capital C), substituted directly.
- Added Tier 3 Master Clothier (slot 2) stocking 3-4 GM/Exceptional-quality pieces per chosen style in natural, non-rare cloth dye-tub hues (`ClothingQuality.Exceptional` — clothing's own distinct quality enum, not `ArmorQuality`/`WeaponQuality`, so a new `ApplyClothingQuality` helper was added rather than extending the existing `ApplyExceptional`), priced per category (Hats 70-90gp, Shirts/Pants 50-75gp, Misc Attire 60-100gp, Footwear 55-90gp — ranges reverse-derived from the ticket's own worked price anchors).
- Added Tier 4 High-Fashion & Rare Hue Specialist (slot 3+) stocking 1-2 pieces per chosen style in a curated 8-hue rare palette (Pure Black, Ice White, Ice Blue, Neon Pink, Blaze, Neon Green, Charcoal, Acid Green) at the same category price ranges plus a flat +300gp surcharge. Two data gaps in the ticket's own hue table resolved: Ice White uses this file's own already-established, corrected `0x480` (the ticket's own `0x481` is the exact off-by-one value an SP-031 comment on this same const already flagged as wrong); Charcoal and Neon Pink each gave two overlapping options that collided with Pure Black and each other, so one non-colliding value was kept per hue so all 8 stay distinct.
- Added a 2% independent restock roll for a curated-rare-hue Dye Tub, priced `Utility.RandomMinMax(100, 300) * 1000` (100,000-300,000gp in 1,000gp steps) and sold loose in root rather than boxed.
- Vendor titles updated to the ticket's own tier names: slot 2 "the Clothier" → "the Master Clothier", slot 3+ "the Master Dyer" → "the High-Fashion & Rare Hue Specialist". No slot-mapping/roll-logic changes were needed — `DetermineEffectiveSlot`/`StockVendor` already give every archetype the universal 1-vendor 50/50 Tier1/Tier2 roll and sequential Tier1-4 multi-vendor cascade generically.
- Stack sanitization: every garment and the rare Dye Tub is explicitly `Amount = 1` via `ApplyClothingQuality`/direct assignment.
- Verified live via a temporary self-invoking test hook (removed before final deploy; no in-game GM client available headless, same `BaseHouse.AllHouses`-backed real-vendor pattern as recent tickets' own hooks): Tier 3 confirmed with all 5 boxes present, every garment `Amount=1` and `ClothingQuality.Exceptional`, zero rare hues used, and the Pants box correctly holding all 3 real types despite the ticket's "3-4" request; Tier 4 confirmed with all 5 boxes present, every garment `Amount=1`/Exceptional/curated-rare-hued, and every price including the full +300gp surcharge; a 300-pass loop (the whole roll is one 2%-chance event) observed the Rare Dye Tub with a curated hue, a price in `[100000, 300000]` on an exact 1,000gp step, and `Amount=1`. Zero exceptions across the whole pass. `dotnet build` — 0 Errors/0 Warnings. `install-server.sh update --force` deployed cleanly; final boot showed zero exceptions across a 90-second stability window.

### SP-057: Combined Tinker/Carpenter House (Tiers 1-4 Classic Recipe Calibration)

- Rebuilt `StockTinkerCarpenter` end to end onto 4 flat (no sub-container) tiers, each stocking loose from a dedicated pool via a new shared `StockToolPool` helper: Tier 1 Master Tinker (12 of 22 tool/gadget types, 3-4 units each), Tier 2 Apprentice Carpenter (12 of 13 furniture/storage types, 3-4 units each, zero `BaseAddonDeed`), Tier 3 Master Woodworker (12 of 15 shelving/instrument/utility types, 3-4 units each, zero `BaseAddonDeed`), Tier 4 Architect & Outfitter (8 of 16 addon-deed types, 2-3 units each). 12/8 varieties sit at or under the container-flattening policy's own ">12 varieties" box exception, consistent with this archetype's own pre-existing all-root layout (Tiers 1 and 2's old stock were already loose; this ticket's own Tier 3/4 content is new).
- **Tier 1/2 content swap**: the ticket's own tier order puts tools at slot 0 and furniture at slot 1 — the reverse of this file's pre-existing slot 0 = Carpenter/slot 1 = Tinkerer mapping — so the two tiers' stocking logic (and their apparel: the SmithHammer-holding apron now dresses slot 1, the TinkerTools-holding apron now dresses slot 0) were swapped to match, renamed `TinkerApprenticeCarpenterApparel`/`TinkerMasterTinkerApparel` respectively.
- **Class-name verification against the live ModernUO source** caught 8 names in the ticket's own pools that don't match a real class 1:1, each resolved to its real, verified equivalent: Tier 2's "WoodenStool"→`Stool`, "Footstool"→`FootStool` (capital S), "SmallTable"→`PlainLowTable` (no generic small-table class exists; `LargeTable` already covers the large side), "WritingDesk"→`WritingTable` (matches this file's own pre-existing Carpenter pool); Tier 3's "Bookcase"→`FullBookcase`, "OpenKeg"→`Keg` (no "open" variant exists), "Drum"→`Drums` (plural); Tier 4's "DartOffDeed"→`DartBoardEastDeed`/`DartBoardSouthDeed` (neither of the ticket's own two guesses was correct). The ticket's own "AbbatoirDeed" spelling was confirmed correct against this file's pre-existing usage (`AbattoirDeed` does not exist).
- **Tier 4 directional deeds**: every deed in ModernUO ships as East/South pairs except `SmallForgeDeed`/`PentagramDeed`/`AbbatoirDeed`/`BallotBoxDeed` (no direction variant). For Anvil/DartBoard/SmallBed/LargeBed — the 4 pairs the ticket itself flags with "(East or South deed)" — one pool entry rolls between both variants independently per unit at spawn time via a target-typed conditional expression, rather than doubling the pool into 20 separate entries.
- **Pricing**: every Tier 1-4 price is the ticket's own explicit number, deliberately superseding SP-046's older master-list figures for the same deeds where they differ (e.g. `SmallForgeDeed` 1,860g → 1,490g, `StoneOvenEastDeed` 4,020g → 1,730g) — this ticket's own table takes precedence per its own explicit instruction.
- Stack sanitization: every tool, furniture piece, instrument, and deed is explicitly `Amount = 1` inside `StockToolPool` itself, so every pool (present and future) gets the guardrail automatically rather than needing it repeated at each call site.
- Verified live via a temporary self-invoking test hook (removed before final deploy; no in-game GM client available headless, same `BaseHouse.AllHouses`-backed real-vendor pattern as recent tickets' own hooks): Tiers 1-3 each confirmed with exactly 12 distinct types, `Amount=1` throughout, zero sub-containers (Tier 1) and zero addon deeds (Tiers 2/3); Tier 4 confirmed with exactly 8 distinct deed *roles* (grouped by `Name`, since — exactly as designed — 2 of the 4 East/South-randomized pool entries legitimately produced both directional C# types across their 2-3 unit copies in this run, `GetType()` count 10 vs. `Name` count 8, confirmed as the expected design rather than a bug), every item `Amount=1` and a genuine `BaseAddonDeed`; a 40-try loop confirmed the universal 1-vendor 50/50 roll still lands on both Tier 1 and Tier 2. Zero exceptions across the whole pass. `dotnet build` — 0 Errors/0 Warnings. `install-server.sh update --force` deployed cleanly; final boot showed zero exceptions across a 90-second stability window.

### SP-058: Tier 1 Master Tinker GM/Exceptional Quality Assignment

- `TinkerToolPool` (SP-057) now carries a `ToolQualityKind` tag (`None`/`Tool`/`Weapon`) per entry, consumed by a new dedicated `StockTinkerToolPool` stocking path for Tier 1 only — Tiers 2-4 keep using the plain, quality-agnostic `StockToolPool` helper unchanged, since only Master Tinker's tools/weapons carry quality in this ticket.
- Real inheritance verified against the live ModernUO source rather than trusting the ticket's own "BaseTool" example groupings, which misclassified 2 items: `Pickaxe` is actually a `BaseWeapon` (`BaseAxe`), not a `BaseTool`, and `Scissors` is a plain `Item` with no quality mechanism at all — neither a `BaseTool` nor a `BaseWeapon`. `Shovel` is `BaseHarvestTool` rather than `BaseTool` directly, but independently declares the exact same `Quality`(`ToolQuality`)/`Crafter` shape, so it's grouped under `Tool`. Final classification: 10 `Tool` (Shovel, SewingKit, MortarPestle, ScribesPen, TinkerTools, Tongs, Hammer, Saw, DovetailSaw, DrawKnife), 4 `Weapon` (Pickaxe, ButcherKnife, Cleaver, SkinningKnife), 8 `None` (Lockpick, Scissors, Key, KeyRing, Clock, Sextant, Spyglass, Globe).
- New `ApplyToolQuality` helper sets `Quality = ToolQuality.Exceptional` and `Crafter` on `BaseTool`/`BaseHarvestTool` items — a FOURTH distinct quality enum in this file (alongside `ArmorQuality`/`WeaponQuality` via the existing `ApplyExceptional`, and SP-056's `ClothingQuality` via `ApplyClothingQuality`), since neither `BaseTool` nor `BaseHarvestTool` shares a quality mechanism with `BaseArmor`/`BaseWeapon`/`BaseClothing`. `Weapon`-tagged items reuse the existing `ApplyExceptional` (already handles `BaseWeapon` correctly). `None`-tagged items just get `Name` set, matching the ticket's own "remain standard instances" instruction.
- All items retain `Amount = 1` (set unconditionally before the quality branch) and flat root backpack placement (`SellLoose`) — no container/layout changes.
- Verified live via a temporary self-invoking test hook (removed before final deploy; no in-game GM client available headless, same `BaseHouse.AllHouses`-backed real-vendor pattern as recent tickets' own hooks): a 15-pass loop over Tier 1's random 12-of-22 pick observed all 22 pool items at least once (10 Tool / 4 Weapon / 8 None distinct types, matching the classification exactly), confirmed every `BaseTool`/`BaseHarvestTool` item was `ToolQuality.Exceptional` with a non-empty `Crafter`, every `BaseWeapon` item (Pickaxe and all 3 knives) was `WeaponQuality.Exceptional` with a non-empty `Crafter`, every plain-`Item` type had its `Name` set with no quality applied, `Pickaxe` was confirmed classified as `Weapon` and never as `Tool`, and `Scissors` was confirmed classified as `None`. Zero exceptions across the whole pass. `dotnet build` — 0 Errors/0 Warnings. `install-server.sh update --force` deployed cleanly; final boot showed zero exceptions across a 90-second stability window.

### SP-059: Fisher, Baker & Maritime House (Tiers 1-4 Pre-T2A Calibration)

*(Ticket text itself was headed "SP-058," colliding with the immediately-prior tool-quality ticket already logged under that number above - entered here as SP-059 to keep the changelog's own numbering unambiguous.)*

- Implemented 4-tier stocking for `MarketArchetype.FisherCurioBaker` with strict root-only backpack displays — the old Tier 1/2's "Basket of Cooked Ribs (50)"/"Basket of Meat Pies (50)" packaged subcontainers are gone, along with the entire old Deep Sea Fisher/Treasure Hunter/Curio Antiquarian content they and the other two tiers used to sell.
- Tier 1 Master Baker & Provisioner: provisioned cooked ribs, cooked bird, bread loaves, meat pies, apple/fruit pies, and muffins/pizzas in calibrated bulk stacks (2-3 stacks of each size per type, exact ticket prices).
- Tier 2 Fishmonger & Cartographer: raw/cooked fish steaks in 100/500-count stacks, 3-4 loose `FishingPole` units (guardrail: no durability properties, resource tags, or MIB/SOS/green-net mechanics - plain pre-T2A pole only), and all 4 crafted map tiers (Local/City/Sea Chart/World).
- Tier 3 Cellar Master & Tavern Provisioner: kegs of ale/wine/cider, a roast pig, sacks of flour, cheese wheel and honey jar stacks, and beverage pitchers randomly filled with Liquor/Ale/Wine.
- Tier 4 Grand Shipwright & Nautical Explorer: all 3 boat deeds (Small/Small Dragon/Medium), 1-2 trophy Big Fish (weight explicitly rolled 100-200 stones per the ticket, overriding the class's own wider 3-200 default), spyglasses, and sextants.
- Enforced pre-T2A guardrails and clamped unstackables — every deed, keg, roast pig, sack of flour, pitcher, big fish, and map is `Amount = 1`; every food/fish stack routes through the existing `Stack()` helper, which already clamps any non-stackable item to `Amount = 1` as a guardrail.
- **Class-name verification against the live ModernUO source** caught 9 names in the ticket's own pools that don't match a real class 1:1: "CookedRibs"→`Ribs` (already this file's own pre-existing name), "PeachPie"→`FruitPie` (the real second/non-apple pie class), "Muffin"→`Muffins` (plural), "Pizza"→`CheesePizza`/`SausagePizza` (no generic "Pizza" class exists; one is picked at random per stack), "SackOfFlour"→`SackFlour`, "JarOfHoney"→`JarHoney` (both word-order swaps), "BeveragePitcher"→`Pitcher` (takes a `BeverageType` arg, the same pattern this file's pre-existing `Jug(BeverageType)` calls already use), and "SmallShipDeed"/"SmallDragonShipDeed"/"MediumShipDeed"→`SmallBoatDeed`/`SmallDragonBoatDeed`/`MediumBoatDeed` (ModernUO names every deed "Boat," never "Ship" - confirmed against `Multis/Boats/`, requiring a new permanent `using Server.Multis;` in this file since these classes live outside `Server.Items`). No dedicated beverage-filled "keg" class exists anywhere in the engine (only the magic-only `PotionKeg` and the plain decorative `Keg` container) — the real `Keg` furniture class is used directly for the 3 drink kegs, individually named per drink via `Named()`; it remains a real `Container` class under the hood, but is sold empty as its own standalone product (the same precedent SP-057 already set selling `WoodenChest`/`SmallCrate` as furniture products) rather than used as an organizer wrapping other stock, so it doesn't violate the root-only policy's actual intent.
- Vendor titles updated to the ticket's own tier names, with Tier 1/2 apparel swapped to match the new tier content (the HalfApron/Cap combo now dresses the Baker at slot 0, the FloppyHat/FishingPole combo now dresses the Fishmonger at slot 1 - the reverse of the old Deep Sea Fisher/Tavern Cook order), renamed `FisherMasterBakerApparel`/`FisherFishmongerCartographerApparel`/`FisherCellarMasterApparel`/`FisherGrandShipwrightApparel`. No slot-mapping/roll-logic changes were needed — `DetermineEffectiveSlot`/`StockVendor` already give every archetype the universal 1-vendor 50/50 Tier1/Tier2 roll and sequential Tier1-4 multi-vendor cascade generically.
- Verified live via a temporary self-invoking test hook (removed before final deploy; no in-game GM client available headless, same `BaseHouse.AllHouses`-backed real-vendor pattern as recent tickets' own hooks): all 4 tiers confirmed with zero *organizer* sub-containers (a dedicated check distinguishing the 3 legitimate standalone-product Kegs from a real wrapping organizer, after an initial false FAIL from a stricter "any Container" check was diagnosed and fixed the same way SP-052's own analogous false-FAIL was); Tier 1 confirmed exact Ribs 50-stack and BreadLoaf 40-stack prices; Tier 2 confirmed the fishing pole/all 4 map types at `Amount=1` and the RawFishSteak 500-stack price; Tier 3 confirmed 6-9 kegs present, `RoastPig`/`SackFlour`/`Pitcher` all `Amount=1`, and every Pitcher's beverage type inside the requested `{Liquor, Ale, Wine}` set; Tier 4 confirmed all 3 boat deed prices, `BigFish` weight inside `[100, 200]`, and every item `Amount=1`; a 40-try loop confirmed the universal 1-vendor 50/50 roll still lands on both Tier 1 and Tier 2. Zero exceptions across the whole pass. `dotnet build` — 0 Errors/0 Warnings. `install-server.sh update --force` deployed cleanly; final boot showed zero exceptions across a 90-second stability window.
